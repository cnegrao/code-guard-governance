# Handoff — M1A-R1 Wave A Ambiguity Resolution

Date: 2026-09-01

## Purpose

Carry forward the reviewed M1A-R1 semantic decisions and the exact local validation state without relying on chat or recovery transcripts as project authority.

## Outcome

- All 7/7 reviewed fields are resolved.
- Existing comments classified `KEEP` were left untouched.
- The work is metadata-only and has not been applied to a database.

## Wave A migration

Exact path: `supabase/migrations/20260901021520_wave_a_metadata_comments.sql`

Local static validation:

- 1 `COMMENT ON TABLE` statement.
- 92 `COMMENT ON COLUMN` statements.
- 93 comment statements total.
- 0 non-comment executable SQL statements.
- The migration is untracked and pending manual review.

## Finalized decisions

- `gov_repo.governance_users.preferred_mfa`: legacy, unused, and non-authoritative; not MFA enrollment, verification, enforcement, authorization, or identity-provider synchronization authority.
- `gov_repo.governance_roles.can_approve_up_to`: legacy, unused, and non-authoritative; structured permissions and workflows are authoritative.
- `gov_repo.governance_ledger.actor_session_id`: legacy, deprecated, and non-authoritative; not authentication, authorization, JWT/session, actor identity, or ledger-integrity evidence.
- `gov_repo.mandates.mapped_policies`: legacy, denormalized, and non-authoritative.
- `gov_repo.policy_mandate_mappings`: authoritative normalized representation of policy-to-mandate relationships in the current `gov_repo` model.
- `gov_repo.governance_users.email`, `gov_repo.governance_ledger.qes_anchor_at`, and `gov_repo.policy_mandate_mappings.coverage_level`: retain their repository-evidence-backed hardened semantics.

## Open security finding

HIGH and not remediated: `gov_repo.policy_mandate_mappings` has authenticated read access using `USING (true)` and no `organisation_id`, creating potential cross-tenant metadata exposure if it is reachable through grants or API exposure.

## Working-tree state at handoff

```text
?? codex-recovery-6101-6240.txt
?? docs/codex/
?? supabase/migrations/20260901021520_wave_a_metadata_comments.sql
```

The recovery transcript remains untouched and non-authoritative. Nothing is staged, committed, or pushed.

## Next gate

Stop for manual review of the local migration and the continuity package. No database, Supabase, runtime, authentication, RLS, or schema operation is authorized by this handoff.
